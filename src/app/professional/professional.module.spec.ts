import { professionalModule} from './professional.module';

describe('Professionalmodule', () => {
  it('should create an instance', () => {
    expect(new professionalModule()).toBeTruthy();
  });
});
